query mediaItem ($id: Int) { # Define which variables will be used in the query (id)
  Media (id: $id) {
    ...MediaListFragment
    episodes
    isAdult
    bannerImage
    description
    status
  }
}
